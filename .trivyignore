# .trivyignore
#
# tomcat-embed-core: version is managed by the spring-boot-dependencies BOM
# Consuming apps control the effective version through the BOM.
# Fixed in tomcat 10.1.59+ / 11.0.25+ - remove these when the spring-boot BOMs catch up.
CVE-2026-65182 # Apache Tomcat: Security constraint bypass due to improper access control
CVE-2026-65905 # Apache Tomcat: Authentication bypass via limited replay attack in DIGEST authenticator
CVE-2026-68525 # Apache Tomcat: Unauthorized resource access via FORM authentication bypass
